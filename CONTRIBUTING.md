# 分支与 PR 规则

本项目由一人负责产品决策与验收，AI 协助实现、审查和整理测试。以下规则来自工作计划 v2.1 第 5 节。

## 分支

- `main` 始终可部署；不直接向 `main` 推送功能代码（W0 Day 4 起由分支保护强制）。
- 同一时间只有**一条**主功能分支。
- 命名：`feature/W<week>-<seq>-<slug>`，例如 `feature/W0-01-scope-freeze`、`feature/W1-03-login-session`。
  - `<week>`：计划周次（0–9）
  - `<seq>`：该周内的两位序号
  - `<slug>`：小写英文，用 `-` 连接
- 修复线上问题：`fix/<slug>`；纯文档：`docs/<slug>`。

## 提交

- 每个工作日至少留下一份可审查的 diff 和真实运行结果。
- 提交信息用祈使句概述改动，正文写原因；不提交密钥、`.env` 或本地数据。

## PR

PR 描述必须包含：

1. **范围**：做了什么、明确没做什么
2. **风险**：涉及认证、路径、租户隔离、发布、数据删除时单独列出
3. **测试证据**：运行的命令和结果
4. **手动验证**：真实浏览器中走过的步骤
5. **回滚方法**：如何撤回；涉及迁移时写明回退方式

合并条件：

- CI 全绿 + 本人审阅
- 纯文档 PR 可以豁免 staging 和集成测试
- 认证、路径、租户隔离、发布相关改动**必须**有针对性的测试

## 每日节奏

- 每天只有一个可检查的主结果；只有确实需要时才做 staging 验证。
- 超出当天预算时，优先保住安全边界、文件完整性和恢复路径；UI 细节、格式化、排序功能往后放。
- 当天结束时在 `docs/devlog/` 记录：完成了什么、实际运行结果、遇到的阻塞和下一步。

## 分工

- AI：准备实现、测试、威胁清单和文档。
- 人：审阅所有变更；验证真实浏览器流程；操作生产密钥、域名、备份和部署；决定开放范围。

## 从旧项目引入代码

新仓库不继承旧项目的 git 历史、PR 或云资源。迁移旧代码时必须逐项评审后重新加入，并在 PR 中记录来源、许可证、风险和本项目中的测试结果。
