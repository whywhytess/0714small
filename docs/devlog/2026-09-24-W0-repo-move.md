# 2026-09-24 · W0 · 迁移到新仓库

## 发生了什么

- 最初在 `whywhytess/small0714` 上完成了初始提交和 PR #1（W0 foundation）。这些提交的信息里带有 `Co-Authored-By: Claude` trailer，PR 描述里也有 “Generated with Claude Code”。
- 本人要求提交不带任何 Claude 署名，规则写进 `CLAUDE.md`。
- 要删掉已经合并到 main 的署名，就得强制推送改写 main，而且 GitHub 会一直保留 PR 引用（`refs/pull/1/head`）。本人决定直接换一个新仓库：**`whywhytess/0714small`**。

## 新仓库的历史

- `main` 是旧仓库 main 改写后的版本：**只改了提交信息**（去掉 trailer），文件内容完全相同（tree `c2be44d`）。合并提交里的 “Merge pull request #1” 指的是旧仓库的 PR。
- 推送前检查：gitleaks 扫描没有发现泄露；没有禁止提交的文件名；提交信息里的署名数为 0。

## 本 PR 的改动

- 新增 `CLAUDE.md`：提交和 PR 都不加 Claude 署名，只用本人配置的 Git 身份提交。
- `docs/runbooks/github-setup.md` 和 Go module 路径改为 `whywhytess/0714small`。
- 早期 devlog 里记录的旧仓库地址属于历史记录，保持不变。

## 旧仓库

`whywhytess/small0714` 仍然是公开的，里面有带署名的提交。是否删除或归档，由本人决定。

## 下一步

本 PR 合并后：配置分支保护（`docs/runbooks/github-setup.md`），然后开一个故意失败的 PR，验证它不能被合并。
