# 2026-09-24 · W0 Day 1 · 新仓库与范围冻结

## 主结果

新仓库的基础文档已就位，MVP Scope Freeze v1 草案进入仓库。

## 完成

- 新建 git 仓库（默认分支 `main`），`origin` 指向 `https://github.com/whywhytess/small0714.git`；远程仓库目前为空
- `README.md`：项目目标、计划架构、目录约定、文档索引
- `.gitignore`：排除 `.DS_Store`、`.env*`（保留 `.env.example`）、构建产物和本地数据
- `CONTRIBUTING.md`：分支命名、提交、PR 必填项、合并条件、每日节奏
- `docs/TASKS.md`：W0–W9 任务板
- `docs/scope/mvp-scope-freeze-v1.md`：必须完成、明确延期、安全不变量、里程碑、验收故事、待决事项
- 工作计划 PDF 移到 `docs/plan/`

## 旧项目密钥检查

**未执行。** 本机这个目录里只有新计划 PDF，没有旧项目的代码；旧仓库的位置也还不知道。需要本人在旧仓库里执行：

```sh
git status --short                      # 未提交文件
git stash list
grep -rEn "(AKIA|SECRET|PASSWORD|TOKEN|PRIVATE KEY)" <未提交文件>
```

发现密钥时：留在旧仓库处理（轮换密钥），**不要**复制到新仓库。

## 运行结果

纯文档改动，没有可运行的代码。

## 阻塞 / 风险

- 本机未安装 Node、pnpm、Go、Docker、gh，W0 Day 3 之前需要装好
- git 未配置 `user.name` / `user.email`，首次提交前需要配置
- 待决：两个注册域名、S3 与 PostgreSQL 供应商（Day 2 ADR）

## 下一步

W0 Day 2：写关键 ADR（域隔离、site_id 与对象键、Go Gateway、PostgreSQL Session、S3、不可变部署），以及最小威胁表。
